<!DOCTYPE html>
<html lang="en">
  {% include "partials/header.html" %}
  <body>
    <div class="container">

      <div class="page-header">
        <h1>Shoe Store</h1>
      </div>

      <div class="panel with-nav-tabs">

        <div class="panel-heading">
          <ul class="nav nav-tabs nav-justified">
            <li><a href="/">Home</a></li>
            <li class="active"><a href="/brands">Brands</a></li>
            <li><a href="/stores">Stores</a></li>
          </ul>
        </div>

        <div class="panel-body">
          <div class="row">
            <h3>Welcome to your SQL Shoe Store Manager!</h3>
            <div class="col-xs-6">
              <h3>List of Brands:</h3>
              <ul>
                {% for brand in brands %}
                  <li><a href="edit_brand/{{brand.getId}}">{{ brand.getName }}</a></li>
                {% endfor %}
              </ul>
            </div>
            <div class="col-xs-6">
              <h3>New Brand:</h3>
              <p>Add a new brand in the form below.</p>
              <form action="/add_brand/{{ brand.getId }}" method="post">
                <div class="form-group">
                  <label for="add-brand">Add the brand's name:</label>
                  <input class="form-control" name="add-brand" type="text" placeholder="Nike">
                </div>
                <button type="submit" name="form-submit">Click to submit!</button>
              </form>
              <p>Or you can delete all the brands by clicking the button below.</p>
              <button action="/delete_all_brands" method="post">Delete here!</button>
            </div>
          </div>
        </div>

      </div>

    </div>
  </body>
</html>
