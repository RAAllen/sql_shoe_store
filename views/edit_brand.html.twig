<!DOCTYPE html>
<html lang="en">
  {% include "partials/header.html" %}
  <body>
    <div class="container">

      <div class="page-header">
        <h1>Shoe Store</h1>
      </div>

      <div class="panel with-nav-tabs">

        <div class="panel-heading">
          <ul class="nav nav-tabs nav-justified">
            <li><a href="/">Home</a></li>
            <li class="active"><a href="/brands">Brands</a></li>
            <li><a href="/stores">Stores</a></li>
          </ul>
        </div>

        <div class="panel-body">
          <div class="row">
            <h3>Welcome to your SQL Shoe Store Manager!</h3>
            <div class="col-xs-6">
            <p>Add stores that carry your brand in the form to the right. Or go back to the main brands page by clicking the button below.</p>
            <button action="/brands" method="post">Click me!</button>
            <p>Or you can delete this brand by clicking the button below.</p>
            <button></a href="delete_brand">Delete here!</a></button>
            </div>
            <div class="col-xs-6">
              <form action="/add_store_to_brand/{{ brand.getId }}" method="post">
                <div class="form-group">
                  <label for="new-store">Add a new store's name:</label>
                  <input class="form-control" name="new-store" type="text" placeholder="Fred's Footwear">
                </div>
                <label for="store_id">Or add an existing store that carries your brand:</label>
                <select name="store_id" class="form-control">
                  <option value="0"></option>
                  {% for store in stores %}
                  <option value="{{store.getId}}">{{store.getName}}</option>
                  {% endfor %}
                </select>
                <button type="submit" name="form-submit">Click to add!</button>
              </form>
            </div>
          </div>
        </div>

      </div>

    </div>
  </body>
</html>
